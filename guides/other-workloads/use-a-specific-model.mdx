---
title: "Use a specific model"
description: "Pin a pipeline call to a specific backing model when the pipeline exposes a model parameter."
---

<Note>
  This page is a placeholder. It will be a task-focused recipe for using the `model` parameter on pipelines that expose model choice — when to pin, how to pin, and what trade-offs each model represents.
</Note>
