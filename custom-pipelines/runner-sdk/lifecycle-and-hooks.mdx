{/* Seed content exists at https://github.com/livepeer/livepeer-python-gateway/blob/main/docs/runner-sdk.md — do NOT pull it in during this scaffold pass. We will adapt it in a later content pass. */}
---
title: "Lifecycle and hooks"
description: "How the runner starts, runs, and stops your pipeline — and which hooks you can override."
---

<Note>
  This page is a placeholder. It will eventually cover the runner lifecycle (init → ready → running → drain → exit) and the hook surface available to authors.
</Note>
