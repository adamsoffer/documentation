---
title: "From Terminal"
description: "Authenticate and call the Livepeer API with curl or an SDK."
---

<Note>
  This page is a placeholder. It will eventually walk through getting an API key, making a curl request, and switching to an SDK.
</Note>

<Steps>
  <Step title="Get an API key">
    Placeholder — create a key in the dashboard and store it locally.
  </Step>
  <Step title="Make your first call with curl">
    Placeholder — invoke a hosted pipeline endpoint from a terminal.
  </Step>
  <Step title="Switch to an SDK">
    Placeholder — install the Node or Python SDK and re-run the call.
  </Step>
  <Step title="Inspect the run">
    Placeholder — view the run's status, output, and usage.
  </Step>
</Steps>
