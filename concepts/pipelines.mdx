{/* Canonical definition for "pipeline" on the Developers side. First mention from anywhere on this surface should link here. */}
---
title: "Pipelines"
description: "What a pipeline is — the code- and transport-level implementation of a callable capability — and the difference between batch and live pipelines."
---

<Note>
  This page is a placeholder. It is the canonical Developers-side definition of "pipeline": the code- and transport-level implementation (FastAPI / trickle endpoints + lifecycle hooks) that a developer calls. Distinguishes pipelines from models (which a pipeline runs) and from capabilities (the network advertisement of a pipeline).
</Note>
