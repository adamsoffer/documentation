{/* Canonical definition for "model" on the Developers side. First mention from anywhere on this surface should link here. */}
---
title: "Models"
description: "What a model is on Livepeer, when pipeline-vs-model choice matters, and how to read the model coverage matrix."
---

<Note>
  This page is a placeholder. It is the canonical Developers-side definition of "model" — the variant or backing model a pipeline runs (when applicable). Most pipelines run a single model; some expose a `model` parameter that affects output, latency, and price. This page covers when that decision matters and how to make it.
</Note>
